services:
    php:
        image: jakubboucek/lamp-devstack-php:8.4
        environment:
            TZ: Europe/Prague
        restart: unless-stopped
        volumes:
            - ".:/var/www/html"
